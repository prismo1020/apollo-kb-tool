name: Apollo KB Automation

on:
  workflow_dispatch:
  schedule:
    - cron: "*/10 * * * *"

permissions:
  contents: write

jobs:
  process-corrections:
    runs-on: ubuntu-latest
    steps:
      - name: Check out repository
        uses: actions/checkout@v4
        with:
          fetch-depth: 0

      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: "3.12"

      - name: Install dependencies
        run: pip install -r requirements.txt

      - name: Process correction queue
        env:
          SUPABASE_URL: ${{ secrets.SUPABASE_URL }}
          SUPABASE_SERVICE_ROLE_KEY: ${{ secrets.SUPABASE_SERVICE_ROLE_KEY }}
          CHATBASE_API_KEY: ${{ secrets.CHATBASE_API_KEY }}
          CHATBASE_KB_EDITOR_BOT_ID: ${{ secrets.CHATBASE_KB_EDITOR_BOT_ID }}
          CHATBASE_FILE_IDENTIFIER_BOT_ID: ${{ secrets.CHATBASE_FILE_IDENTIFIER_BOT_ID }}
          CHATBASE_KB_FILE_CREATOR_BOT_ID: ${{ secrets.CHATBASE_KB_FILE_CREATOR_BOT_ID }}
          GITHUB_REPOSITORY: ${{ github.repository }}
        run: python scripts/process_supabase_corrections.py
